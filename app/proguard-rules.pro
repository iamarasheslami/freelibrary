# Add project-specific ProGuard rules here as dependencies are introduced.
# Hilt/Dagger generates code that ProGuard's default Android rules already handle correctly,
# so no Hilt-specific rules are needed here yet.